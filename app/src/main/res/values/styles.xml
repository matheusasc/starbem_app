<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="containerComponents">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">210dp</item>
        <item name="android:background">@drawable/fnd_texto_superior</item>
        <item name="android:layout_margin">20dp</item>
    </style>

    <style name="emoteButton">
        <item name="android:background">"@android:color/transparent"</item>
        <item name="android:layout_marginStart">20dp</item>
        <item name="android:layout_marginTop">132dp</item>
    </style>

    <style name="buttonAtividade">
        <item name="android:layout_width">180dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:layout_margin">20dp</item>
        <item name="android:layout_marginTop">32dp</item>
        <item name="android:background">@drawable/button_background</item>
    </style>

    // Nav Bar
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/black</item>
    </style>

    <style name="bottomNav">
        <item name="itemIconTint">@drawable/item_selector</item> <!-- Controle de cor do ícone -->
        <item name="itemRippleColor">@android:color/transparent</item> <!-- Desabilitar ripple -->
        <item name="android:layout_width">150dp</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:layout_marginBottom">40dp</item>
    </style>

</resources>